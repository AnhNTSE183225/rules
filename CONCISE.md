MUST be concise as much as possible. MUST use bullet points when possible.
Never load entire files. Do NOT auto-include files contents in the prompt.
IF more context is needed from a file path I provide, you use grep/sed/awk command to extract <= 200 lines
